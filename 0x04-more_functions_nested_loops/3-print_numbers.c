#include "main.h"

/**
 * print_numbers -  checks for checks for a digit (zero through 9).
 *
 * Return: Always zero.
 */
void print_numbers(void)
{
	int c;

	for (c = 48; c < 58; c++)
	{
	_putchar(c);
	}
	_putchar('\n');
}
