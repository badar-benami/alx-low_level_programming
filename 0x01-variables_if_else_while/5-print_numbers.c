#include <stdio.h>

/**
 * main - Prints numerals ranging from 0 to 9.
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
