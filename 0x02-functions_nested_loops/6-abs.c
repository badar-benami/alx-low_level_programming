#include "main.h"

/**
 * _abs-computes an integer's absolute value.
 * @n:the integer to be checked.
 * Return:integer's absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
