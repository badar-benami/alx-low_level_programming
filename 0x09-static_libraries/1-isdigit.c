#include "main.h"
		
/**
 * _isdigit - checks for a digit (zero through 9).
 * @c: int to be checked.
 * Return: one if c is a digit, zero otherwise.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
