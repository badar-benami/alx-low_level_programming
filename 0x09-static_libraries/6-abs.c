#include "main.h"

/**
 * _abs---calculates an integer's absolute value.
 * @n:the int to verify.
 * Return:the whole amount of int.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
